{% extends "base.html.jinja2" %}


    {% block navigation_bar %}
        <div class="nav_panel" >
            <div class="div_aligned">
                <img src="/static/images/logo_imt.jpg" class="logo" alt="Logo Imt Atlantique">
                <label class="research_bar">Search for IMT'stagramers:
                    <input type="text">
                </label>
            </div>
            <div class="div_aligned" style="margin-right: 250px">
                <button>Search</button>

            </div>
            <div class="div_aligned">
                <ul>
                    <li style="display:inline">
                        <a href=>
                            <div class="clickable_icon div_aligned">
                                Profile
                            </div>
                            <div class="clickable_icon div_aligned">
                                <a href="{{ url_for('main.index') }}" class="navbar-item">
                                    Home
                                </a>
                            </div>
                            <div class="clickable_icon div_aligned">
                                {% if current_user.is_authenticated %}
                                    <a href="{{ url_for('main.profile') }}" class="navbar-item">
                                        Profile
                                    </a>
                                {% endif %}
                                {% if not current_user.is_authenticated %}
                                <a href="{{ url_for('auth.login') }}" class="navbar-item">
                                    Login
                                </a>
                                {% endif %}
                            </div>
                            <div class="clickable_icon div_aligned">
                            {% if not current_user.is_authenticated %}
                                <a href="{{ url_for('auth.signup') }}" class="navbar-item" style="display: inline-block">
                                    Sign Up
                                </a>
                            {% endif %}
                            {% if current_user.is_authenticated %}
                                <a href="{{ url_for('auth.logout') }}" class="navbar-item">
                                    Logout
                                </a>
                            {% endif %}
                            </div>
                        </a>
                    </li>
                </ul>
            </div>

        </div>
    {% endblock %}

    {% block main_page %}
        <div class="main_page">
            {% for influencer in influencers -%}
                <table class="photos_list">
                    <tr>
                        <dl class="photos_list">
                            <dt size="4pt"><b>{{ influencer.username }}</b></dt>
                            <dd>{{ influencer.email }}</dd>
                        </dl>
                    </tr>
                </table>
            {% endfor %}
        </div>
    {% endblock %}